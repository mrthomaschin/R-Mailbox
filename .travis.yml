language: c

language: python

python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

before_install:
  - sudo apt-get update -qq

#dependencies
before_install:
  - python -m pip install pip==9.0.3

install:
  - pip install -r requirements.txt
  - sudo apt-get install -qq gcc-avr binutils-avr avr-libc

env:
  travis encrypt TWILIO_SID=os.getenv("TWILIO_ACCOUNT_SID")
  travis encrypt TWILIO_AUTH=os.getenv("TWILIO_AUTH_TOKEN")
  
=======

#run tests
script:
  - pytest
  - make

branches:
  - master
  - tchin006/AWS_Atmega
  - cgray009/functionality
  - noahij05/RPi_facerec_fingerprint
  - mdeso001/Atmega_IR_Servo
  - jhalvorson6687/RPiReplySMS_Twilio
  - jhalvorson6687-patch-1
  - jhalvorson6687/unit_tests
  - mdeso001_jhalvorson6687/notifications

