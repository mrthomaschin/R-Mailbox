language: c

language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

#dependencies
install:
  - pip install -r requirements.txt
  
env:
  travis encrypt TWILIO_SID="TWILIO_ACCOUNT_SID"
  travis encrypt TWILIO_AUTH="TWILIO_AUTH_TOKEN"
  
#run tests
script:
  - pytest

branches:
  only:
    - master
